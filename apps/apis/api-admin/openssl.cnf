openssl_conf = default_conf

[default_conf]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
# Permitimos protocolos antiguos explícitamente
MinProtocol = TLSv1
# Bajamos el nivel de seguridad al mínimo posible (0)
CipherString = DEFAULT@SECLEVEL=0
